# Commit C19: PosReceipt Entity & Validator

## Goal
Implement the data model for POS receipts.

## Proposed Changes
- Update `packages/core/src/modules/pos/data/entities.ts` with `PosReceipt`.
- Update `packages/core/src/modules/pos/data/validators.ts` with `posReceiptSchema`.

## Verification
- Run `npm run modules:prepare`.
