# Commit C23b: Session Management UI

## Goal
Implement administrative views for POS sessions.

## Proposed Changes
- Implement `PosSessionList` page in `pos/backend/sessions/page.tsx`.
- Create `PosSessionDetails` view for reviewing session totals and movements.
- **Architectural Requirement**: Follow `@.ai/skills/backend-ui-design` for `DataTable` and `Detail` page layouts.
- **Architectural Requirement**: Proper ACL guards via `page.meta.ts`.

## Verification
- Visual verification of session list and detail views.
