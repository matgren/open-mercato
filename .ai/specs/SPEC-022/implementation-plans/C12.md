# Commit C12: PosCart Entity & Validator

## Goal
Implement the data model for POS carts.

## Proposed Changes
- Update `packages/core/src/modules/pos/data/entities.ts` with `PosCart`.
- Update `packages/core/src/modules/pos/data/validators.ts` with `posCartCreateSchema` and `posCartUpdateSchema`.

## Verification
- Run `npm run modules:prepare`.
