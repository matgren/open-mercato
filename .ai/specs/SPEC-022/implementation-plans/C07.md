# Commit C07: PosSession Entity & Validator

## Goal
Implement the data model for POS sessions.

## Proposed Changes
- Update `packages/core/src/modules/pos/data/entities.ts` with `PosSession`.
- Update `packages/core/src/modules/pos/data/validators.ts` with `posSessionOpenSchema` and `posSessionCloseSchema`.

## Verification
- Run `npm run modules:prepare`.
