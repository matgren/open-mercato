# Commit C16: PosPayment Entity & Validator

## Goal
Implement the data model for POS payments.

## Proposed Changes
- Update `packages/core/src/modules/pos/data/entities.ts` with `PosPayment`.
- Update `packages/core/src/modules/pos/data/validators.ts` with `posPaymentSchema`.

## Verification
- Run `npm run modules:prepare`.
