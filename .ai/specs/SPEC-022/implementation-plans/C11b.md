# Commit C11b: PosCashMovement API Routes

## Goal
Expose cash movement management via REST API.

## Proposed Changes
- Create `packages/core/src/modules/pos/api/cash-movements.ts`.
- Implement `POST /api/pos/cash-movements` endpoint.
- **Architectural Requirement**: API must export `openApi` documentation.
- **Architectural Requirement**: Strict `organizationId` and `tenantId` validation in API handlers.

## Verification
- API tests (e.g., using `supertest` or manual Curl) for recording cash-in/out.
