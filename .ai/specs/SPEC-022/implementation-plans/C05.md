# Commit C05: PosRegister Commands

## Goal
Implement business logic for register management.

## Proposed Changes
- Create `packages/core/src/modules/pos/commands/registers.ts`.
- Implement `pos.register.create`, `pos.register.update`, and `pos.register.delete` commands.
- **Architectural Requirement**: Ensure reversible logic using `CommandHandler` with before/after snapshots in `buildLog`.
- **Architectural Requirement**: Strictly enforce tenant isolation by filtering all queries by `organizationId`.

## Verification
- Unit tests for each register command.
