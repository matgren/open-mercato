# Commit C06c: Auth Module PIN Support

## Goal
Add support for encrypted/hashed 4-6 digit PINs in the `auth` module to enable quick cashier switching.

## Proposed Changes

### [auth]

#### [MODIFY] [entities.ts](file:///Users/maciejgren/Documents/OM/packages/core/src/modules/auth/data/entities.ts)
- Add `pin` column (hashed string) to the `UserCredential` or `User` entity (as per SPEC-022).
- Ensure PIN is handled securely (never in plaintext).

#### [MODIFY] [validators.ts](file:///Users/maciejgren/Documents/OM/packages/core/src/modules/auth/data/validators.ts)
- Add Zod validation for PIN (4-6 digits).

## Verification
- Run `npm run modules:prepare` to check for module errors.
- Verify `auth` module compiles.
