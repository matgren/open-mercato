# Commit C01: Backend Scaffolding

## Goal
Establish the module structure for `pos` in `packages/core`.

## Proposed Changes
- Create `packages/core/src/modules/pos/` directory.
- Create `packages/core/src/modules/pos/index.ts` with basic module metadata.
- Create `packages/core/src/modules/pos/ce.ts` for capability exports.

## Verification
- Verify module discovery by running `npm run modules:prepare`.
