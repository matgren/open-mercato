# Commit C23: Register Management UI

## Goal
Implement administrative views for POS registers.

## Proposed Changes
- Implement `PosRegisterList` page in `pos/backend/registers/page.tsx`.
- Implement Create/Update forms for registers using standard CRUD components.
- **Architectural Requirement**: Use the standard `backend/[page].tsx` and `page.meta.ts` pattern.
- **Architectural Requirement**: Follow `@.ai/skills/backend-ui-design` for `DataTable` and `CrudForm` usage.

## Verification
- Visual verification of register table and forms.
