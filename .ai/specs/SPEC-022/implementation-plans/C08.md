# Commit C08: PosSession Commands

## Goal
Implement session lifecycle logic (Open/Close).

## Proposed Changes
- Create `packages/core/src/modules/pos/commands/sessions.ts`.
- Implement `pos.session.open` and `pos.session.close` commands.
- Note: These are non-reversible to maintain audit integrity, but **MUST** capture snapshots in `buildLog` for audit trail.

## Verification
- Unit tests for session open/close logic.
