# C08: PosSession Commands

## Plan

Implement commands for `PosSession` entity:

- `CreatePosSession`
- `UpdatePosSession`
- `DeletePosSession`
- `OpenPosSession`
- `ClosePosSession`

Follow the pattern established in `C05: PosRegister Commands`.
Ensure Zod validation for inputs.
Use `PosSession` entity in `packages/core/src/modules/pos/data/entities.ts`.
