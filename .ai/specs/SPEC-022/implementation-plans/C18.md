# Commit C18: Sales Module Bridge & Mapping

## Goal
Establish the data mapping and integration logic between POS and Sales modules.

## Proposed Changes
- Create `packages/core/src/modules/pos/lib/salesBridge.ts`.
- Implement mapping functions from `PosCart` and `PosPayment` to `SalesOrder` schemas.
- **Architectural Requirement**: Mapping must handle tax calculations and line item translations correctly.
- **Architectural Requirement**: NO direct ORM relationships; use business keys and FK IDs.

## Verification
- Unit tests for mapping functions (Input POS Cart -> Output Sales Order data).
