# Commit C22: Frontend Scaffolding

## Goal
Establish the frontend module for POS.

## Proposed Changes
- Create `packages/core/src/modules/pos/backend/` for route definitions.
- Create `packages/core/src/modules/pos/components/` for POS-specific UI logic.
- **Architectural Requirement**: Follow the `workflows` module structure for separating routing from heavy interactive components.
- Register the module in the main application shell (`apps/mercato/src/modules.ts`).

## Verification
- Navigating to `/backend/pos/checkout` shows a blank page without 404.
