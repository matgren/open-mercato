# Commit C21: Final Verification & Migration

## Goal
Finalize backend module and generate database migrations.

## Proposed Changes
- Run `npm run modules:prepare` to finalize entity references.
- Generate initial migration for all `pos_*` tables.
- Verify module index and capability exports (`ce.ts`).

## Verification
- Successful build of `packages/core`.
- Database migration applies without errors.
