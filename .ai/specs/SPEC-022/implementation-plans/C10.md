# Commit C10: PosCashMovement Entity & Validator

## Goal
Implement tracking for cash movements.

## Proposed Changes
- Update `packages/core/src/modules/pos/data/entities.ts` with `PosCashMovement`.
- Update `packages/core/src/modules/pos/data/validators.ts` with `posCashMovementSchema`.

## Verification
- Run `npm run modules:prepare`.
