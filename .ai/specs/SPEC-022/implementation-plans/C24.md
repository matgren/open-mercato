# Commit C24: Checkout Layout Skeleton

## Goal
Implement the core layout for the POS terminal.

## Proposed Changes
- Create `PosCheckoutPage` in `pos/backend/checkout/page.tsx`.
- Implement a 2-column layout as per SPEC-022 §8.2.
- Integrate the session header and basic navigation controls.
- **Architectural Requirement**: Follow the `workflows/visual-editor` pattern for managing complex interactive state in a full-page view.
- **Architectural Requirement**: Follow `@.ai/skills/backend-ui-design` for core layout patterns (`Page`, `PageHeader`, `PageBody`).
- **Architectural Requirement**: Use `EmptyState` for empty product grid/cart views.

## Verification
- Visual verification of the terminal layout skeleton.
