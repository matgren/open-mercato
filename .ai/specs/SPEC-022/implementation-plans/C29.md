# Commit C29: Receipt & Reports UI

## Goal
Implement post-sale interactions and reporting.

## Proposed Changes
- Create `PosReceiptDialog` to show receipt options after checkout.
- Implement the `PosSessionReport` view for end-of-day reconciliation.
- **Architectural Requirement**: Follow `@.ai/skills/backend-ui-design` for receipt detail views using `FormHeader mode="detail"`.

## Verification
- View a receipt after a sale and verify the session report totals.
