# Commit C03: Database Seeding

## Goal
Seed required entities in other modules (SalesChannel, PaymentMethods).

## Proposed Changes
- Create `packages/core/src/modules/pos/setup.ts`.
- Implement logic to seed `SalesChannel` with code `'pos'`.
- Implement logic to seed `SalesPaymentMethod` for `'cash'` and `'card'`.

## Verification
- Run setup logic and verify database entries.
