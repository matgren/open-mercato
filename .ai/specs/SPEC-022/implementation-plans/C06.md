# Commit C06: PosRegister API Routes

## Goal
Expose register management via REST API.

## Proposed Changes
- Create `packages/core/src/modules/pos/api/registers.ts`.
- Implement GET, POST, PUT, DELETE handlers for registers.
- **Architectural Requirement**: Export `openApi` for doc generation and `metadata` with `requireAuth` and `requireFeatures`.
- **Architectural Requirement**: Strictly enforce tenant isolation (filter queries by `organizationId`).
- **Architectural Requirement**: Use `makeCrudRoute` with `indexer` where applicable.

## Verification
- API integration tests for register endpoints.
