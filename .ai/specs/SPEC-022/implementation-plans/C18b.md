# Commit C18b: PosCart Completion Command

## Goal
Implement the final transactional command for cart completion.

## Proposed Changes
- Implement `pos.cart.complete` command in `packages/core/src/modules/pos/commands/carts.ts`.
- Utilize mapping logic from C18 to create `SalesOrder` and associated records.
- Emit `pos.cart.completed` domain event.
- **Architectural Requirement**: Wrap the entire operation in a single database transaction.
- **Architectural Requirement**: Validate that the session is open and the cart has items.

## Verification
- Unit tests for the completion command, verifying event emission and Sales module integration.
