# Commit C09: PosSession API Routes

## Goal
Expose session operations via API.

## Proposed Changes
- Create `packages/core/src/modules/pos/api/sessions.ts`.
- Implement `/api/pos/sessions/:id/open` and `/close` endpoints.
- Export OpenAPI documentation.

## Verification
- API integration tests for session lifecycle.
