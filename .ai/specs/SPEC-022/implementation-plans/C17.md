# Commit C17: PosPayment Commands

## Goal
Implement logic for recording payments.

## Proposed Changes
- Create `packages/core/src/modules/pos/commands/payments.ts`.
- Implement `pos.payment.record` command.
- Add logic to calculate `changeAmount` (as per SPEC-022 §6.1b).

## Verification
- Unit tests for various payment scenarios (overpayment, exact amount).
