# Commit C02: ACL & Events Definitions

## Goal
Define permissions and domain events for the POS module.

## Proposed Changes
- Create `packages/core/src/modules/pos/acl.ts` with feature definitions (using singular naming convention: `pos.cart.manage`, etc.).
- Create `packages/core/src/modules/pos/events.ts` with initial event types.

## Verification
- Check type safety and alignment with SPEC-022 §9.
