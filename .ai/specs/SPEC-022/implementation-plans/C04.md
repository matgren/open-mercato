# Commit C04: PosRegister Entity & Validator

## Goal
Implement the data model for POS registers.

## Proposed Changes
- Create `packages/core/src/modules/pos/data/entities.ts` with `PosRegister`.
- Create `packages/core/src/modules/pos/data/validators.ts` with `posRegisterSchema`.

## Verification
- Run `npm run modules:prepare` to generate metadata.
