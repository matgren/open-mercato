# Commit C14: PosCartLine Commands

## Goal
Implement business logic for cart line management.

## Proposed Changes
- Create `packages/core/src/modules/pos/commands/cart-lines.ts`.
- Implement `pos.cart.line.add`, `pos.cart.line.update`, and `pos.cart.line.delete`.
- **Architectural Requirement**: Commands must be reversible using `CommandHandler` with snapshots.
- **Architectural Requirement**: Ensure tenant isolation for all cart operations.

## Verification
- Unit tests for adding, updating, and removing items from a cart.
