# Commit C28: Payment Selection UI

## Goal
Implement the payment method selection and entry interface.

## Proposed Changes
- Create `PosPaymentPanel` for selecting payment methods (Cash/Card).
- Implement Cash entry with quick-amount buttons.
- **Architectural Requirement**: Follow `@.ai/skills/backend-ui-design` for `Dialog` usage with `embedded={true}` for payment forms.
- **Architectural Requirement**: Adhere to keyboard navigation standards (numpad support, `Enter` to confirm).

## Verification
- Visual verification of the payment method selector and amount input.
