# Commit C13: PosCartLine Entity & Validator

## Goal
Implement the data model for POS cart lines.

## Proposed Changes
- Update `packages/core/src/modules/pos/data/entities.ts` with `PosCartLine`.
- Update `packages/core/src/modules/pos/data/validators.ts` with `posCartLineSchema`.

## Verification
- Run `npm run modules:prepare`.
